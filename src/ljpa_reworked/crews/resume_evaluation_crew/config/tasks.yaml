evaluate_resume_task:
  description: >
    Evaluate candidate profile for:
    Title: {title}
    Vacancy: {text}
    LinkedIn: {linkedin_url}

    Match candidate profile skills and experience to vacancy.
    Calculate rating (0-100) and actionable summary recommendations.
    Do not access external URLs.
  expected_output: >
    A BasicEvaluationCrewAI object with summary (str) and rating (int 0-100).
  agent: resume_evaluation_agent